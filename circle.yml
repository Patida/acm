deployment:
  staging:
    branch: master
    codedeploy:
      acm:
        application_root: /
        region: eu-ireland
        revision_location:
          revision_type: S3
          s3_location:
            bucket: appcimo
            key_pattern: acm-{BRANCH}-{SHORT_COMMIT}
